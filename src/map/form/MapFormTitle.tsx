import { palette } from "@utils/palette";

const MapFormTitle = () => {
  return (
    <div
      css={{
        paddingLeft: "4px",

        fontSize: "12px",
        color: palette.gray200,
      }}
    >
      나의 장소 등록
    </div>
  );
};

export default MapFormTitle;
