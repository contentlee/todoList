import { palette } from "@utils/palette";

const RegisterTitle = () => {
  return (
    <div
      css={{
        paddingLeft: "4px",

        fontSize: "12px",
        color: palette.gray200,
      }}
    >
      키테고리 등록
    </div>
  );
};

export default RegisterTitle;
